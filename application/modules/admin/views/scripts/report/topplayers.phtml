<?php 
echo $this->form;

if (count($this->topplayers))
	{
		$linkArray = array();
		foreach($this->topplayers as $playerdata)
		{
			$linkArray[$playerdata['PlayerId']] = array(
										'controller' => 'player',
										'action' => 'view',
										'playerId' => $playerdata['PlayerId']);
		}
		$this->table($this->topplayers, array('PlayerId'), $linkArray);
	}
?>